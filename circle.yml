test:
  override:
    - ./test_projects.sh:
         parallel: true

machine:
 environment:
   TEST_DB_USER: "ubuntu"
   TEST_DB_NAME: "circle_test"
 java:
   version: oraclejdk8
 services:
   - mysql
